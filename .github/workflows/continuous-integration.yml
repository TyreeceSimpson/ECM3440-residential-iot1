name: Continuous Integration

on:
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  Integrate:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Test with pytest and report coverage
      run: |
        pytest --cov-report term-missing:skip-covered --cov=ECM3440-residential-iot1 tests/ --cov-fail-under=75
    - name: Check linting with pylint
      run: |
        pylint actuator-one actuator-two sensor-one actuator-two --exit-zero
    - name: Analyse code with radon cyclomatic complexity
      run: |
        radon cc actuator-one actuator-two sensor-one actuator-two -a